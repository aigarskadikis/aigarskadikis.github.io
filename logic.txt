
detect backend
detect argument
  if argument not set then
    pick database name from backend config
  if this is not backend server and no argument given then exit app

configure logic:
  ENGINE=MYSQL or ENGINE=POSTGRESQL
if both configs are persistent then last value will be forced. In summary:
  only one database can be listed at a time.
  only one database engine can be listed at a time.

Test database connection by using universal query 'select version()'
  if the test fails then exit program.
  othervise print database engine version on screen

List the count of tables
  if count of tables is less than 80 then exit the app

Pick up the version of zabbix database
  all future queries will be based on which version you have 

Detect database partitioning being used

